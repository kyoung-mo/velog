<pre><code class="language-c">#define _CRT_SECURE_NO_WARNINGS
#include &lt;stdio.h&gt;

int main() {

    char c1;
    char* pc1;

    pc1 = &amp;c1;

    int i1;
    int* pi1;

    pi1 = &amp;i1;

    printf(&quot;&amp;c1 = %d, &amp;(c1+1) = %d\n&quot;, pc1, pc1+1);
    printf(&quot;sizeof &amp;c1 = %d, size of &amp;c2 = %d\n&quot;, sizeof(pc1), sizeof(pc1+1));
    printf(&quot;\n&quot;);
    printf(&quot;&amp;i1 = %d, &amp;(i1+1) = %d\n&quot;, pi1, pi1+1);
    printf(&quot;sizeof &amp;i1 = %d, size of &amp;(&amp;i1+1) = %d\n&quot;, sizeof(pi1), sizeof(pi1+1));

    return 0;
}</code></pre>